import React from 'react';

const MyComponentFunc = ({name, number}) => {
    return(
        <div>
            <h2>I'm 함수형 컴포넌트 {name}  / {number}</h2>
        </div>
    )
}

export default MyComponentFunc;